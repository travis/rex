{
  "name": "rex",
  "version": "0.0.1",
  "description": "rex rules them all",
  "main": "dst/server.js",
  "dependencies": {
    "apollo-server": "^2.0.0-rc.10",
    "apollo-server-express": "^2.0.0-rc.10",
    "express": "^4.16.3",
    "graphql": "^0.13.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "nodemon": "^1.18.3"
  },
  "scripts": {
    "clean": "rm -rf dst && mkdir dst",
    "build": "npx babel src --out-dir dst",
    "start": "node dst/server.js",
    "watch": "nodemon src/server.js --exec babel-node",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch-tests": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
