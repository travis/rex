{
  "name": "rex-schema",
  "version": "0.0.1",
  "description": "The schema for the rext GraphQL server",
  "main": "schema.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "graphql": "^0.13.2",
    "graphql-tag": "^2.9.2"
  }
}
